despre noua versiune a create root si avantaje de peromanta ; link spre articol ?
se poate scoate   method: 'GET', din fetch
basic reguli pentru hooks 
    start with use 
    need to be first in a function
    need to use it in a function (exemplu daca il scot din app componenent)
const { useState } = React in mod normal ar fi import useState from React
<script crossorigin src="https://unpkg.com/babel-standalone@6/babel.min.js"></script> este necesar pentru JSX ?

in loc de onChange sa pui useRef?
pas 1: add <form> ...
pas 2 <input onChange={ e => console.log(e.target.value) } />
pas 3: const [subject, setSubject] = useState('')
    onChange={ e => setSubject(e.target.value) }
    fetch('https://icanhazdadjoke.com/search?term=' + subject
     <p>{subject}</p>
    it does not work, it does not do a fetch
pas 4: add useEffect pentru useEffect(() => { getData()}, [subject])
poti sa remove form-ul de aici, nu e musai



great if you want to start with react in 2024 , or if you want a refresher

on errror?
 
    